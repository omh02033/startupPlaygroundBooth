import { styled } from '#/stitches.config';
import { Hexile } from '@/components';

export const Wrapper = styled(Hexile, {
  padding: '3rem 9rem',
  borderRadius: '5rem',
  fontSize: '4rem',
  color: '$white2',
  fontWeight: 700,
  background: '$orange',
});
